# Node dependencies (installed automatically by Render)
node_modules/
npm-debug.log*

# Local SQLite database (keep user data out of Git)
wallets.db
wallets.db-journal

# Environment variables & secrets
.env
*.env
